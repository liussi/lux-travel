import { HeroSection, Subtitle, Title } from './HeroSection.styled';

const Hero = () => {
  return (
    <HeroSection>
      <Subtitle><span>the</span>
      <span>world’s</span>
      <span>most</span></Subtitle>
      <Title><span>extra</span><span>ordinary</span><span>places</span></Title>
    </HeroSection>
  );
};

export default Hero;